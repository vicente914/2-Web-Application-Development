{
 "version": "0.2.0",
    "configurations": [
    {
        "name": "Escuchar Xdebug (Docker)",
        "type": "php",
        "request": "launch",
        "port": 9003,
        "pathMappings": {
            "/var/www/projects": "${workspaceFolder}/projects"
        }
    }
 ]
}